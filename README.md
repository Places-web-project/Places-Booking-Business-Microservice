# Places Booking & Business Microservice

Spring Boot 3 microservice for teams, rooms, desks and bookings CRUD.

## Local run

```bash
./mvnw spring-boot:run
```

Default port: `8082`

## Environment variables

See `.env.example`.
